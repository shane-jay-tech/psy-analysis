# 精确版本锁 — 用于交付/CI/复现 bug
# 用法：pip install -r requirements-lock.txt
# 维护：以 D:\code\psy-analysis 当前已验证环境为基准，发版时手动更新。
# 与 requirements.txt 区别：
#   - requirements.txt：软约束（开发/快速安装），允许小版本浮动
#   - requirements-lock.txt（本文件）：精确锁定，用于交付/CI/复现 bug
# 若该锁版本与新 Python / 新依赖冲突，先更新 requirements.txt 的范围验证可用，
# 再 pip freeze 重新生成本文件。
# 最后更新：2026-07-02（基于已验证 .venv 环境）

streamlit==1.57.0
pandas==3.0.3
numpy==2.4.4
scipy==1.17.1
statsmodels==0.14.6
pingouin==0.6.1
plotly==6.7.0
jieba==0.42.1
openpyxl==3.1.5
pyreadstat==1.3.4
openai==2.36.0
python-docx==1.2.0
fpdf2==2.8.7
factor-analyzer==0.5.1
scikit-learn==1.5.2
semopy==2.3.11
PyYAML==6.0.3
kaleido==1.3.0
pypdf==6.14.2
pywebview==6.2.1
