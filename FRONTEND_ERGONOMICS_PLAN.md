# 前端人体工学优化实施计划

## 固化目标

在保留现有功能、用户数据与项目兼容性的前提下，自动完成前端人体工学优化：以 Windows 笔记本和桌面端为主，建立统一的暖白、深蓝、低饱和状态色设计系统，规范字体、间距、圆角、阴影、按钮、表单、卡片与反馈；重构首页、侧栏、数据分析、向导和交付页的信息层级，默认聚焦核心任务并折叠进阶内容，减少滚动、误触和认知负担；兼顾窄屏、键盘、对比度、触控与减少动画，保持 Streamlit 架构和原有入口；通过浏览器走查、视觉截图、UI回归、无障碍与性能测试验收，未完成事项进入路线图。

## 推荐决策

- 面向 1366–1920px Windows 笔记本/桌面，兼顾 768px 窄屏。
- 采用现代学术工具风格：暖白背景、深蓝主色、低饱和语义色。
- 保持 Streamlit 架构、页面名称、功能入口和工作区格式兼容。
- 默认突出当前核心任务，专业设置与低频说明渐进披露。
- 不复制商业产品视觉资产，不引入需要联网的字体或前端依赖。

## 实施步骤

1. 在浏览器中记录首页、侧栏、数据分析、向导和交付页的视觉基线。
2. 建立集中式设计令牌与全局组件样式，消除主页面零散 CSS。
3. 优化首屏层级、侧栏分组、内容宽度、表单节奏、反馈与操作命中区。
4. 增加 1366px、窄屏、键盘焦点、对比度和减少动画适配。
5. 运行静态契约、Streamlit AppTest、核心 UI 流程、性能与完整发布门禁。
6. 浏览器复查并形成前后对比、残余问题和后续路线图。

## 验收标准

- 全局颜色、间距、圆角、阴影、字体和状态反馈来自单一设计系统。
- 首屏主任务清晰，侧栏可扫描，常用控件至少 44px，正文行宽适合阅读。
- 1366px 无横向溢出；窄屏内容可用；键盘焦点清楚；减少动画设置生效。
- 现有导航、上传、分析、向导、交付与项目数据契约不变。
- 浏览器视觉复查通过，UI/无障碍/性能测试无新增失败。

## 非目标与路线图

- 本轮不重写为 React/Vue，不替换 Streamlit，不做品牌 Logo 重设计。
- 不一次性重排所有低频专业模块；按设计系统继承，例外项进入路线图。

## 实施与验收结果

- 已建立集中式设计系统与 Streamlit 主题，统一主色、语义色、字体、间距、圆角、阴影和组件状态。
- 已优化侧栏、模板中心、数据分析、文献工作台、选题漏斗、向导路径卡片和交付中心，并清除重复标题。
- 已修复窄屏侧栏收起后仍占 300px、向导步进器显示 Markdown 符号及高饱和完成度徽章等实际问题。
- 浏览器验证 1280×720、768×900、390×844 均无横向溢出；关键文字对比度为 5.32–15.31:1。
- 完整发布门禁通过：2491 passed、59 skipped；UI 黄金流、导出、统计、隐私、恢复、性能预算全部通过。

## 后续路线图（不阻塞本轮交付）

1. 在 CI 安装 Playwright 浏览器，加入关键页面截图差异基线，替代当前环境中被跳过的原生浏览器用例。
2. 分批迁移深层低频结果卡、实验高级设置中的历史内联颜色，使其完全由设计令牌驱动。
3. 根据真实用户遥测和访谈评估可选的高对比模式、紧凑密度与深色主题，不在缺少证据时提前增加设置负担。
