# 系统级优化实施计划

## 固化目标

在保留现有功能、用户数据与项目兼容性的前提下，完成一次可验收的系统级优化：以中国心理学学习者和研究者“选题—文献—设计—数据—分析—写作—交付”的核心流程为主线，统一导航、术语、反馈与操作逻辑，减少重复入口、等待和学习成本；修复高频缺陷，完善错误恢复、撤销确认、自动保存、新手引导与无障碍体验；提升启动、交互、大数据处理和导出的稳定性与性能；复核统计方法、效应量、置信区间、APA7输出及证据引用，强化隐私脱敏、AI降级和学术诚信边界；收敛配置与模块职责，清理重复代码，统一版本与文档。为关键流程补齐单元、集成、UI及端到端测试，以黄金研究流程、全量离线测试、性能基线、兼容性检查和可回滚变更作为验收标准；本轮优先解决高影响问题，其余形成后续路线图。

## 已确认决策

- 保留工作区中现有的 v5.8/v5.9 未提交改动，并将其视为本轮基线。
- 采用有限、可交付的一轮优化：全面审计，优先实施高影响且可验证的改动。
- 采用“新手友好默认流程 + 专业用户进阶能力”的双层体验。
- 不覆盖既有 `PLAN.md`（该文件属于测试覆盖审计任务）。

## 范围

- 核心研究闭环的入口、导航、状态反馈、错误恢复和交付体验。
- 启动、交互、自动保存、大数据处理与导出路径的性能和稳定性。
- 统计结果、隐私、AI 降级、学术诚信等高风险边界。
- 配置、版本、文档、重复代码和关键流程测试的一致性。

## 非目标

- 不推倒重写 Streamlit 架构，不引入破坏性数据迁移。
- 不在缺少证据时新增偏离核心研究闭环的大功能。
- 不修改或清理用户生成的数据、缓存、归档与既有未提交成果。
- 不承诺一次消除所有历史技术债；未入选项进入排序后的路线图。

## 实施步骤

1. 建立基线：检查工作区差异、版本、测试收集、性能与关键黄金流程。
2. 多视角审计：分别检查用户体验、可靠性/性能、架构/维护性与高风险边界。
3. 按“用户影响、风险、证据、实现成本、可验证性”选出本轮改动。
4. 先补回归测试，再以兼容方式实现；重叠文件逐段核对，避免覆盖既有修改。
5. 运行目标测试、全量离线测试、性能 smoke、版本/文档一致性检查。
6. 独立评审完整差异，修复关键发现，形成升级报告与后续路线图。

## 验收标准

- 至少打通并验证一条离线黄金研究流程，AI/网络不可用时有明确降级。
- 本轮触及的缺陷或体验问题均有自动化测试或可复现验证证据。
- 全量离线测试通过；若有跳过项，记录原因与残余风险。
- 性能 smoke 不出现新增 FAIL，关键路径不低于当前基线。
- 版本号、入口文案、报告和已知问题状态一致。
- 不破坏现有项目格式、归档和用户数据；所有改动可通过 Git 差异回滚。

## 风险与安全措施

- 工作区已有大量未提交修改：只做增量编辑，修改前后检查差异，不使用重置或覆盖命令。
- 统计与研究结论属于高风险区域：只接受有黄金数据或独立复核支持的变更。
- 性能优化可能引入并发或持久化竞态：先保守实现并增加故障恢复测试。
- UI 大改可能使熟悉路径的用户迷失：保持入口兼容，优先渐进式收敛。

