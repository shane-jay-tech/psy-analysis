# v5.10 升级报告：研究路径接通 + 安全工作区 + 新手体验恢复

**版本**：v5.9 → v5.10
**日期**：2026-08-24
**目标**：在兼容现有项目与未提交成果的前提下，完成一轮可验收的系统级优化。

## 一句话总结

v5.10 接通了此前无法进入的实验设计模块，把导航收敛为单一事实源；将“清空会话”改造成“保存旧项目后开始新研究”，集中清除可能串入下一研究的论文、证据、图表和导出状态；同时恢复真正的一次性新手引导、接入标准模式下一步提示，并加强自动保存、图表导出、隐私说明和版本一致性。

## 主要改进

### 1. 核心研究路径不再断路

- 主导航统一使用 `src/ui/navigation.py::PAGE_MODES`，消除 app 内重复列表。
- 新增“🧪 实验设计”主入口并接入 `render_experiment_design_ui()`。
- 向导进入/离开问卷、实验和论文模块时采用延迟状态切换，避免 Streamlit 控件把跳转状态覆盖回去。
- 模板中心的下一步文案改为真实存在的主入口。

### 2. 新研究不再混入旧研究资产

- 将原“清除会话数据”改为“开始新研究”：先原子保存当前项目，再创建空白项目。
- 新增集中式 `clear_research_session()`，清除数据、结果卡、论文、证据、图表、方法建议、模板状态、导出授权和缓存键。
- 用户偏好、隐私同意、模型选择和原项目文件保持不变。

### 3. 新手默认简单，专家仍可自由进入

- 移除首次启动强制写入“已完成/已跳过引导”的逻辑。
- 全新用户可看到一次 5 分钟体验；完成或跳过后真实持久化选择。
- 帮助区新增“重新打开 5 分钟新手引导”，已有数据时也可主动回看。
- 标准模式接入最高优先级“下一步”提示，并校验目标页面真实存在。

### 4. 自动保存更可靠、更诚实

- 项目索引和工作区 JSON 改为同目录临时文件写入后原子替换；中断或磁盘异常不再破坏上一份有效文件。
- 自动保存成功时显示真实时间，失败时给出可操作提示；成功后清除旧错误。
- 节流键同时记录项目 ID，快速切换项目不会误跳过新项目的保存。
- 数据上传和分析完成后主动保存；切换模型或开始新研究会取消仍在运行的旧 AI 请求。
- 手动生成快照的时间不再在每次 rerun 时被错误刷新。
- 隐私声明明确说明本地项目会落盘，以及云端 AI 只在用户调用时接收所提交文本。

### 5. 论文图表导出自动恢复

- Kaleido 默认选择的 Edge 无法启动时，自动发现本机 Chrome 或 Playwright Chromium。
- PNG、Word 内嵌图表和批量 ZIP 三条路径共用同一回退。
- 确实缺少浏览器时，统一提示安装 Kaleido/Chrome 或改用 HTML。

### 6. 版本与文档一致

- 版本单一事实源更新为 `5.10.0`，发布日期为 `2026-08-24`。
- 论文中的工具使用声明动态引用 `APP_VERSION_LABEL`，不再写死 v2.2。
- 系统报告同步版本、规模和 2544 条离线测试收集基线。

### 7. 独立评审后的安全收口

- 工作区导入记录内容指纹，同一个上传控件不会在 rerun 后反复覆盖用户的新修改。
- 同名数据文件按 `file_id`/内容身份识别；替换文件解析失败时保留上一份有效数据。
- 大文件使用上传对象的 size，列选择必须由用户确认后才真正加载。
- 模板数据同步进入主分析状态，创建模板后不再出现“已加载”与“请上传”的矛盾。
- 模板创建前先保存旧项目，再创建、持久化并激活真实 UUID 项目；任何前置失败都不会清空当前研究。
- LLM 解析结果在嵌入 HTML 前统一转义；问卷下载移除手写 data URL，改用安全下载控件与净化文件名。
- 问卷 HTML 报告先转义全部用户/模型文本；推荐分析只预填方法，不再猜测列并直接运行。
- 分析缓存以每次执行时的数据内容哈希为准，原地清洗后不会误用旧统计结果。
- 用户偏好也采用临时文件、fsync、原子替换与进程内锁；项目读改写增加进程内串行锁。

## 验证结果

- `python scripts/release_gate.py --mode full`：**全部通过**。
- 全量结果为 **2486 passed、59 skipped、42 warnings**；黄金统计、模板 Golden Flow、交付包结构、APA 表格、Word/ZIP 导出均通过。
- 重点回归（导航、重置、新手引导、保存、版本、图表导出、UI）：**全部通过**。
- 测试收集：**2544 items**（完整门禁排除显式联网抓取套件）。
- 性能 smoke：**0 FAIL，0 WARN**；7 条关键路径均低于预算，冷启动核心导入约 3.1–3.3s。
- 隔离存储的 Streamlit AppTest 黄金流：**8 项通过**；真实 CSV 上传、描述统计执行、导航与实验设计路由均已验证。
- 浏览器 Playwright 核验：15 项非浏览器契约通过、25 项因当前 `.venv` 未安装 Playwright 而跳过；此项不宣称浏览器 E2E 已通过。

### 8. 完成度审计后的高风险收口

- 归档与课程作业包统一使用共享脱敏：高风险 PII 列删除，中低风险标识符稳定哈希并记录脱敏报告。
- 正式交付在生成任何下载按钮前重跑全局门禁；数据、分析结果、论文和研究包任一处存在高风险 PII 都会阻断。
- 损坏的项目索引先隔离为备份，再从现有 workspace 文件重建，避免静默覆盖或项目“消失”。
- 持久化路径集中配置；测试与便携部署可通过环境变量重定向，默认历史布局不变。
- 并发 AI fallback 在线程无 Streamlit 上下文时不再写 UI trace，消除退出阶段后台日志竞态。
- 新增键盘焦点、触控目标、减少动画、高对比和跳转主内容的无障碍支持。

## 剩余路线图

1. 大工作区自动保存仍需同步构建完整 CSV/base64 快照，首次触发可能卡顿；后续考虑增量数据资产或后台快照队列。
2. 论文写作、分析作业包和正式交付包仍有多个出口，需继续统一“草稿导出”和“正式交付”的入口语义。
3. 多应用实例并发写入尚无 OS 级文件锁；当前单进程部署由原子替换与 `RLock` 覆盖。
4. `app.py` 仍是大型编排文件，后续应按页面控制器渐进拆分。
5. 浏览器 Playwright Python 包未安装；要求真实 Chromium 认证的发布环境需补装并重跑。

完整逐项证据与跳过说明见 `docs/SYSTEM_OPTIMIZATION_COMPLETION_AUDIT.md`。
