# 心理学研究工具系统 v2.4 — 本科体验深化自评报告

**日期**: 2026-05-16
**版本**: v2.3 → v2.4
**测试**: 134 → 165（+31 新增），全部通过，零回归

---

## 一、升级概述

本次升级在 v2.3 本科向导模式基础上，聚焦**消除从数据到论文的体验断层**，在不破坏任何现有功能的前提下，深化了十个方向的优化。所有新增功能在本科模式默认关闭时不干扰标准用户。

### 关键指标

| 指标 | v2.3 | v2.4 | 变化 |
|------|------|------|------|
| 测试数量 | 134 | 165 | +31 |
| 新增文件 | — | 1 | — |
| 修改文件 | — | 2 | — |
| 代码新增行 | — | ~550 | — |
| 向导总步骤 | 6 | 7 | +1 |
| 决策树方法 | 7 | 12 | +5 |
| 错误预防类型 | 7 静态 | 7 静态 + 2 行为感知 | +2 |
| 动态术语映射 | — | 12 种分析方法 | — |

---

## 二、任务完成详情

### 一、向导终点接入论文写作（Tasks 1-2）

**文件**: `app.py`（修改，+~180 行）

- 向导从 6 步扩展为 **7 步**，新增 **「第 7 步：写入论文」**
- 步骤 5 分析执行时自动填充 `wizard_results_context`（测试类型、变量名、样本量等）
- 步骤 7 自动生成 APA7 格式的方法和结果段落：
  - **方法部分**：包含被试描述、分析软件、检验方法、显著性水平、效应量报告声明
  - **结果部分**：自动提取统计量（t/F/r + 效应量 + CI），填入 APA7 模板
  - 三个 tab 页：方法 | 结果 | 完整草稿
- 用户可微调：分析方法描述模板（12 种可选）、显著性水平、检验方向、软件名称、被试描述
- 提供「打开完整论文写作模块」按钮，跳转并预填信息
- 学术诚信使用提示（核对统计量、APA格式、软件名替换等 5 条）

### 二、向导↔论文模块数据自动传递（Task 2）

- `wizard_results_context` 字典在步骤 5 运行分析时自动填充
- 步骤 7 从该字典读取，用户无需重复输入变量名、样本量等信息
- 未执行分析时步骤 7 自动拦截并提示「请先完成分析」

### 三、方法决策树 7→12 扩展（Tasks 3-4）

**文件**: `app.py`（向导第 4 步 + 标准模式助手面板）

新增 5 种方法的推荐路径（每人附带白话解释）：

| 新增方法 | 决策树问法 | 推荐触发条件 |
|---------|-----------|------------|
| 中介效应分析 | "自变量通过中间变量影响因变量？" | 选择「检验中介/间接效应」 |
| 调节效应分析 | "某个变量会加强/减弱X对Y的影响？" | 选择「检验调节效应」 |
| 探索性因素分析 | "一组题目背后属于几个潜在维度？" | 选择「探索潜在维度」 |
| 信度分析 | "一组题目稳定测量同一概念？" | 选择「检验量表信度」 |
| 卡方检验 | "自变量和因变量都是类别变量？" | 选择「检验类别变量关联」 |

标准模式「🎯 不知道用什么方法？」面板同步扩展至 12 种，界面完全一致。

### 四、示例数据一键加载（Task 5）

**文件**: `src/data/demo_datasets.py`（新建，~120 行）

- 两组模拟数据生成函数：
  - `generate_demo_questionnaire_data(n=200)` — 10 列含性别/年级/年龄/焦虑总分及三维度/自尊总分及二维度，含~3%缺失值
  - `generate_demo_experiment_data(n_per_group=40)` — 8 列含被试编号/组别/前测后测记忆成绩和反应时
- 向导第 2 步新增「🧪 一键加载示例数据」展开面板
- 两个按钮：📋 问卷示例 / 🧪 实验示例，点击后自动加载到 session_state
- 数据可重现（固定 seed）

### 五、行为感知错误检测（Task 6）

**文件**: `app.py`（修改，~30 行）

- 标准模式下自动记录每次分析到 `analysis_history`（类型、DV、IV）
- **多次 t 检验检测**：连续 3 次以上独立样本 t 检验且 DV 高度重叠 → 弹出红色强警告
  - 提示一类错误膨胀风险
  - 建议改用单因素 ANOVA + 事后检验
  - 仅在非向导模式下触发（向导用户已提前规划分析，不干扰）
- 本科模式下不触发此行为检测

### 六、相关分析后续建议（Task 7）

**文件**: `app.py`（修改，~25 行）

- 执行相关分析后自动检查：变量数 ≥ 3 且存在 |r| > 0.5 → 弹出建议
  - 提示可进一步做中介分析或调节分析
  - 附带分析需求输入提示

### 七、动态术语展示（Task 8）

**文件**: `app.py`（修改，~60 行）

- 12 种分析方法各映射 5 个相关术语（如 t检验→p值/Cohen's d/95% CI/效应量/正态性）
- 分析结果页底部新增「📖 本次分析相关术语」展开区
- 5 列并排展示，每列包含术语名称 + 一句话解释
- 侧边栏静态术语速查表保留不变（完整索引）
- 未识别的分析类型使用默认术语集（p值/效应量/95% CI/检验力/显著性）

### 八、向导↔问卷/实验模块衔接（Task 9）

**文件**: `app.py`（修改，~50 行）

- 向导第 1 步新增「🔗 前置准备」区域：
  - 问卷路径：询问「已有现成问卷？」→ 可选跳转问卷设计模块
  - 实验路径：询问「需设计实验程序？」→ 可选跳转实验设计模块
- 跳转时保存向导完整状态到 `_wizard_return`
- 侧边栏全局检测 `_wizard_return`，在问卷/实验模块中显示「🔙 返回向导继续」按钮
- 点击返回后恢复向导路径、步骤和数据，从第 2 步继续

### 九、测试扩展（Task 10）

**文件**: `tests/test_ui.py`（新增 31 个测试，6 个类）

| 测试类 | 数量 | 覆盖内容 |
|--------|------|---------|
| `TestWizardToPaper` | 5 | 上下文填充、方法模板、结果元素、学术诚信 |
| `TestDecisionTreeExtended` | 6 | 12种方法全路径覆盖验证 |
| `TestDemoDataLoading` | 6 | 问卷列名、类型、实验列名、分组、缺失值、可复现 |
| `TestBehaviorDetection` | 5 | t检验检测、混合方法不触发、不同DV不触发、高相关建议、弱相关不触发 |
| `TestDynamicTerms` | 5 | t检验术语、ANOVA术语、相关术语、信度术语、默认术语 |
| `TestWizardQuestionnaireLink` | 4 | 数据保留、问卷桥选项、实验桥选项、返回清除 |
| **合计** | **31** | |

```
$ pytest tests/ -q
..........................................................................
..........................................................................
...............................                                          [100%]
165 passed, 5 warnings in 32.18s
```

---

## 三、修改文件汇总

### 新建文件（1 个）

| 文件 | 行数 | 功能 |
|------|------|------|
| `src/data/demo_datasets.py` | ~120 | 问卷和实验示例数据生成器 |

### 修改文件（2 个）

| 文件 | 修改内容 |
|------|---------|
| `app.py` | +~550 行。向导第7步论文生成、wizard_results_context、决策树7→12、示例数据加载按钮、行为感知检测、相关后续建议、动态术语、向导↔模块衔接 |
| `tests/test_ui.py` | +31 个测试（6 个类），覆盖论文生成、决策树、示例数据、行为检测、动态术语、模块衔接 |

---

## 四、向后兼容性保证

1. **本科模式默认关闭** — 所有新增 UI 在 `undergrad_mode=False` 时不渲染
2. **标准模式仅增加可选面板** — 方法选择助手为 expander（默认折叠），行为检测在分析后追加不修改结果
3. **公共 API 签名不变** — 无新增或修改任何分析/问卷/实验/论文模块的函数签名
4. **Session state 新增 key 均带合理默认值** — `analysis_history: []`, `_wizard_return: None`
5. **165 测试全部通过** — 134 原有 + 31 新增，零回归
6. **向导 7 步对旧版无影响** — 仅当 `undergrad_mode=True` 时可见

---

## 五、已知局限

1. **论文生成基于简单模板** — 未调用 LLM 进行文本润色，生成的段落较为模板化
2. **行为检测仅追踪 t 检验** — 未扩展到 ANOVA 事后比较、多次相关分析等其他模式
3. **动态术语为静态映射** — 未根据分析结果的实际数值动态调整（如根据效应量大小给出不同解释）
4. **模块衔接数据传递有限** — 仅保留向导状态，未将问卷设计结果直接注入向导

---

## 六、后续建议

1. 论文生成接入 LLM 润色，根据用户的实际数据自动撰写更自然的 APA7 段落
2. 行为检测扩展至 ANOVA 事后比较、多次相关分析、逐步回归等模式
3. 动态术语根据效应量数值给出个性化解读（如 η²=0.15 时标注"大效应"）
4. 模块衔接增强：问卷设计完成后自动将题目信息注入向导数据上传步骤
5. 为示例数据添加更多类型（追踪数据、多层次数据、元分析数据）

---

**结论**：v2.4 本科体验深化已全面完成。所有 10 项任务按规范实现，165 个测试全部通过，零向后兼容性破坏。系统现已具备从数据到论文的完整本科向导流程（7 步），12 种方法的智能推荐决策树，示例数据一键加载，行为感知错误检测，动态术语展示，以及向导与问卷/实验模块的双向跳转衔接。
