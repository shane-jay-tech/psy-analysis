# Psy Analysis v3.0 升级报告

**升级日期**：2026-05-17
**版本定位**：v2.9 用得贯通 → **v3.0 AI 陪练**
**升级范围**：从「论文交付器」到「研究陪练」——三件大事让本科生用得放心、上手即懂、有人指导

---

## 1. 升级核心：质的跃迁，不是数量堆叠

v2.7→v2.9 的迭代解决了**功能完整性**（能用、好用、贯通）。v3.0 跳出"加功能"思维，回应三个真实痛点：

| 痛点 | v3.0 解决方案 |
|---|---|
| 关电脑 = 工作丢失 | **autosave 自动保存**：每次分析/收藏后静默写盘，启动检测自动恢复 |
| 新用户打开不知怎么开始 | **5 分钟引导路径**：首次访问演示数据→自动跑分析→直达成品页 |
| 论文工具只能润色，不能"懂分析" | **AI 助教深度对话**：注入研究上下文，多轮对话指导 |

---

## 2. 任务完成度（4/4）

| # | 任务 | 状态 | 关键模块 | 测试 |
|---|---|---|---|---|
| **T1** | 自动保存（autosave）| ✅ | `src/utils/autosave.py`（节流 30s + 启动恢复 + 元信息） | +10 |
| **T2** | 5 分钟引导路径 | ✅ | `src/ui/onboarding.py`（3 阶段卡片 + 演示数据自动注入） | +7 |
| **T3** | AI 助教深度对话 | ✅ | `src/paper_writer/ai_tutor.py`（system prompt + 多轮 + ollama/openai 兼容） | +13 |
| **T4** | 文档收尾 + 自评估 | ✅ | UPGRADE_REPORT_V3.0.md + auto-memory + 版本号 | — |

---

## 3. 核心功能详解

### 3.1 自动保存（autosave）

**触发点**：
- 向导第 5 步分析完成 → trigger_autosave
- 第 5 步收藏图表 → trigger_autosave
- 节流：同 session 30 秒内不重复写

**存储位置**：`~/.psy_analysis/autosave.json` + `autosave_meta.json`

**启动恢复 UI**：
- 检测到 autosave 文件且当前 session 无数据时，顶部显示恢复卡片
- 列出元信息：保存时间、文件大小、是否含 df / 分析 / 收藏图
- 两按钮：「✅ 恢复上次工作」/「❌ 忽略并清除」
- 用户已加载新数据时不打扰

**API**：
```python
trigger_autosave(session_state, build_workspace_snapshot)  # 调用方一行
render_restore_prompt(st)  # 启动时一行
```

### 3.2 5 分钟引导路径

**3 阶段卡片**（`src/ui/onboarding.py`）：
1. **欢迎 + 选择路径**：渐变橙色卡片，"5 分钟体验" / "直接开始"二选一
2. **加载演示数据**：解释数据结构（社交焦虑问卷 n=200），一键加载
3. **自动运行分析**：提示等价于"向导→选差异→t 检验"，一键运行

**完成后**：
- 自动切到向导第 7 步（论文产出页）
- 顶部显示绿色亮点：4 个可点的功能（Word / 答辩 / 图表 ZIP / 交付包）
- 标记 `onboarding_completed = True`，下次不再显示

**触发条件**：`is_first_visit()` —— 无 df + 无 analysis_history + 未完成/未跳过

### 3.3 AI 助教深度对话

**核心：上下文注入式 system prompt**

```python
你是一位资深心理学研究方法导师...

# 学生当前研究上下文
- 统计方法：独立样本t检验
- 样本量：n = 200
- 因变量：社交焦虑总分
- 自变量：性别

# 学生当前的分析结果
- p 值：p = 0.0240（显著）
- 效应量：Cohen's d = 0.550
- t_statistic：2.413
- df：78
- Levene 方差齐性：通过（p=0.32）

# 请基于以上信息回答学生的问题
（如果信息缺失，请明确说"看不到，能告诉我吗？"）
```

学生不再需要从零解释自己的研究——AI 已经"懂"具体数值，能给针对性回答。

**功能特性**：
- **推荐问题**：5 个针对当前检验类型的高频问题，一键发送
- **多轮对话**：保留最近 10 条历史，避免 token 超限
- **双 provider**：ollama（`/api/chat`）+ OpenAI 兼容（`/v1/chat/completions`）
- **错误友好**：HTTP 错误、JSON 格式异常都抛 `TutorAPIError` 并给中文提示
- **位置**：向导第 6 步（看完结果直接问）+ 第 7 步（写论文时问）独立对话历史

**伦理边界（写在 system prompt）**：
- 不替学生做决定，给 2-3 选项 + 利弊
- 不预测论文能否通过
- 涉及伦理/学术不端时明确拒绝
- 信息缺失时坦诚承认而非编造

---

## 4. 文件改动清单

### 新增文件（5）

| 文件 | 用途 | 行数 |
|---|---|---|
| `src/utils/autosave.py` | autosave 节流 + 恢复 UI | 160 |
| `src/ui/onboarding.py` | 5 分钟引导 3 阶段卡片 + 演示注入 | 180 |
| `src/paper_writer/ai_tutor.py` | system prompt 构造 + LLM 调用 + 推荐问题 | 240 |
| `tests/test_autosave.py` | 节流/状态/往返/异常处理 | 10 测试 |
| `tests/test_onboarding.py` | 首次访问检测 + 标记 | 7 测试 |
| `tests/test_ai_tutor.py` | prompt 注入 + LLM mock + 上下文抽取 | 13 测试 |
| `UPGRADE_REPORT_V3.0.md` | 本文件 | — |

### 修改文件（4）

| 文件 | 改动 |
|---|---|
| `app.py` | 版本号 v2.9→v3.0；启动加 `render_restore_prompt` + `render_onboarding_card` |
| `src/ui/undergrad_wizard.py` | 第 5 步分析完成 → trigger_autosave；第 6/7 步加 `_render_ai_tutor`；第 7 步加 `render_post_demo_highlight` |
| `src/ui/renderers.py` | 收藏图表后 trigger_autosave |

---

## 5. 测试结果

| 指标 | v2.9 | v3.0 | 变化 |
|---|---|---|---|
| 常规测试 | 402 | **432** | **+30** |
| 跳过 | 0 | 0 | — |
| Playwright E2E | 11 | 11 | 0 |
| **总计** | 413 | **443** | **+30** |
| 通过率 | 100% | **100%** | — |
| 警告数 | 0 | 0 | 持续清零 |

**回归命令**：`pytest tests/ -q --ignore=tests/test_playwright_e2e.py` → **432 passed in ~51s**

---

## 6. 已知局限

| 项 | 说明 | 应对 |
|---|---|---|
| autosave 默认 30s 节流 | 极快连续操作可能丢失最后一次 | 切换 wizard 步骤时强制 force=True 保存 |
| autosave 路径写死 ~/.psy_analysis/ | 不支持自定义 | 大多数本科生单用户场景够用 |
| 引导仅覆盖问卷路径 | 实验路径用户需自己进向导 | v3.1 可加实验演示分支 |
| AI 助教对话不持久化 | 关浏览器后历史消失 | 设计上：避免敏感对话长期保存；想保留可手动复制到 markdown |
| AI 助教 token 上限 10 轮 | 长对话早期信息会被截断 | 大多数本科生场景 5 轮内就能解决问题 |

---

## 7. 部署验证清单

- [ ] 启动 `streamlit run app.py`，侧栏显示 **v3.0 · AI 陪练**
- [ ] **首次访问**：看到橙色「👋 欢迎」卡片，可选「5 分钟体验」/「直接开始」
- [ ] 走完 5 分钟体验：自动进入向导第 7 步，顶部绿色亮点提示 4 个可试功能
- [ ] 跑分析后关闭浏览器 → 重开 → 顶部出现「💾 检测到上次自动保存的工作区」
- [ ] 点「✅ 恢复上次工作」→ 数据 + 分析 + 收藏图全部回来
- [ ] 配好 LLM API Key 后，第 6/7 步「💬 问 AI 助教」expander 可用
- [ ] 推荐问题点击 → AI 看到具体的 n、d、p 值后给针对性回答

---

## 8. v3.0 设计哲学

> "用得贯通" → "用得放心 + 上手即懂 + 有人指导"

v2.7→v2.9 解决了**功能矩阵**——本科生需要的所有产出物都能造出来。
v3.0 解决的是**心理负担**：

- **不再担心「忘了保存」** → autosave 全自动
- **不再担心「打开看不懂」** → 5 分钟引导让新用户秒懂
- **不再担心「我的数据 AI 看不懂」** → AI 助教读着具体数值答你

这是**从工具到陪练**的角色转变。本科生写论文时不再孤单——
关掉浏览器有人帮你存，第一次进来有人带路，写不下去时有人陪你想。

---

**升级负责人**：Claude（首席架构师 & 开发工程师）
**用户**：本科生研究者（单用户场景）
**状态**：✅ 升级完成，**432 passed, 0 warnings**
